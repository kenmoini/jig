@extends('layouts.pf4-primary')

@if(!Auth::user()->hasPermission('admin.capabilities.view'))

@section('pageTitle', 'Permission Denied')

@section('content')
<p class="pf-u-text-center">Permission Denied</p>
@endsection
@else

@section('pageTitle', 'Administration - Capabilities')

@section('content')
<table class="pf-c-table pf-m-grid-lg" role="grid" aria-label="Listing of Capabilities" id="capability-table">
  <thead>
    <tr role="row">
      <th class="pf-c-table__sort pf-m-selected" role="columnheader" aria-sort="ascending" scope="col">
        <button class="pf-c-table__button">
          <div class="pf-c-table__button-content">
            <span class="pf-c-table__text">Name</span>
            <span class="pf-c-table__sort-indicator">
              <i class="fas fa-long-arrow-alt-up"></i>
            </span>
          </div>
        </button>
      </th>
      <th class="pf-c-table__sort " role="columnheader" aria-sort="none" scope="col">
        <button class="pf-c-table__button">
          <div class="pf-c-table__button-content">
            <span class="pf-c-table__text">Description</span>
            <span class="pf-c-table__sort-indicator">
              <i class="fas fa-arrows-alt-v"></i>
            </span>
          </div>
        </button>
      </th>
      <th class="pf-c-table__sort " role="columnheader" aria-sort="none" scope="col">
        <button class="pf-c-table__button">
          <div class="pf-c-table__button-content">
            <span class="pf-c-table__text"># Roles</span>
            <span class="pf-c-table__sort-indicator">
              <i class="fas fa-arrows-alt-v"></i>
            </span>
          </div>
        </button>
      </th>
    </tr>
  </thead>
  <tbody role="rowcapability">
    @if(count($capabilities))
      @foreach($capabilities as $capability)
      <tr role="row">
        <td role="cell" data-label="Capability key">{{ $capability->key }}</td>
        <td role="cell" data-label="Capability description">{{ $capability->description }}</td>
        <td role="cell" data-label="Number of roles capability is in">{{ $capability->roles()->count() }}</td>
      </tr>
      @endforeach
    @else
      <tr role="row">
        <td role="cell" class="pf-u-text-align-center" data-label="No records" colspan="3">No records found!</td>
      </tr>
    @endif
  </tbody>
</table>

@endsection


@section('footerScripts')

<script type="text/javascript" src="/assets/js/vendor-jquery.dataTables.min.js"></script>
<script type="text/javascript">
  jQuery(document).ready( function () {
      jQuery('#capability-table').DataTable({
          searching: false,
          ordering:  true
      });
  });
</script>
@endsection

@endif